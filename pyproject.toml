[project]
name = "pcap-tool"
version = "4.0.0"
description = "PCAP analysis tool: network graph, draw.io/Visio diagrams, Excel reporting, and pentest findings"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
